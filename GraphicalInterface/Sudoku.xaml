<UserControl x:Class="GraphicalInterface.Sudoku"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GraphicalInterface"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <UniformGrid x:Name="sudokuGrid"
            Width="{Binding RelativeSource={RelativeSource self}, Path = ActualHeight}"
            MinWidth="{Binding RelativeSource={RelativeSource self}, Path = ActualHeight}"/>
        <Grid Grid.Row="1" Margin="15" Width="350">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition MaxWidth="75" Width="1*"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="sudokuStringValue" MouseDown="sudokuStringValue_MouseDown"></TextBox>
            <Button Grid.Column="1">Copy</Button>
        </Grid>
    </Grid>
</UserControl>
